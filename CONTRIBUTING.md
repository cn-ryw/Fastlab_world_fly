# Contributing

感谢参与改进。提交前请：

1. 从当前默认分支创建功能分支，保持改动聚焦；
2. 不提交模型、原始标定数据、飞行日志、个人路径、凭据或本地工具配置；
3. 运行 `python3 scripts/audit_public_tree.py`；
4. 运行全部 JavaScript 测试和非 GPU pytest；
5. 对自治链改动说明 depth mode、标定身份、验证环境和仍未通过的门禁。

代码与文档应清楚区分“已实现”“已自动测试”“已在 GPU 在线验证”和“真实飞行验证”。
不要把相对深度称为米制深度，也不要把人工保留基线写成自动精度门禁通过。

提交 Issue 或 PR 时请使用模板，删除 token、机器路径、原始轨迹和设备信息。第三方代码、
模型或资产必须同时提供可验证来源、版本和许可证；来源不明的二进制资产不应新增。
